---
alwaysApply: true
---

# Strict Constraint: No Unauthorized AI/Agent Identity（硬性 · 零例外）

人类开发者拥有全部产品思想、决策与署名权。Agent / AI 只是工具，**永远不是作者、合著者或署名方**。

违反本条 = 未完成任务。推送前必须自检；发现署名必须当场剥掉再推。

## 硬性禁止（任意方式均不可）

禁止以**任何形式**添加、保留、暗示 agent / AI / 工具署名或身份，包括但不限于：

1. **Git 提交**
   - `Co-authored-by` / `Signed-off-by` / `Assisted-by` / `Generated-by` / `Acked-by` 等一切 trailer
   - 把 Author、Committer、`--author`、tagger 设为 Cursor / Codex / GPT / Claude / 其它 AI 身份或邮箱（如 `cursoragent@cursor.com`）
   - 提交说明正文或 footer 出现 “with Cursor / AI assisted / generated by …” 等字样
   - 环境若自动注入上述内容：**提交后、推送前必须剥离**（`commit-tree` / 重写 message）；**禁止**带着 agent trailer push

2. **代码与仓库内容**
   - 源码、注释、日志、文档、README、CHANGELOG、LICENSE、PR/Issue 模板中的 AI/agent 署名或致谢
   - 变量名、文件名、badge、水印、metadata 中夹带 agent 身份

3. **绕过**
   - 禁止用「平台自动加的」「方便追溯」「行业惯例」等理由保留署名
   - 禁止在用户未明确要求时，主动提议把 AI 写成贡献者

## 允许的唯一例外

仅当用户**明确要求**对接某第三方 API，且协议/SDK **必须**出现该厂商商品名（如支付渠道文档中的官方产品名）时，可使用该厂商商品名——仍不得写成 “AI 写的 / Cursor 生成”。

## 推送前验收

- `git log` 目标范围内无 `Co-authored-by: Cursor`（及其它 agent trailer）
- Author / Committer 仅为人类开发者配置的 `user.name` / `user.email`
- Contributors 不应再因本仓库提交出现 agent 账号（历史已清则等待 GitHub 缓存刷新）
